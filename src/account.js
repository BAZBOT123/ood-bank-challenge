class Account {
    constructor(accountNo, accountName) {
        this.accountNo = accountNo
        this.accountName = accountName
    }
}

// eslint-disable-next-line no-undef
module.exports = Account
